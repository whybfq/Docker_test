FROM ubuntu:16.04

LABEL author=sanner

ENV PROJECT_DIR=/home

#ENV APP_PORT=3000


WORKDIR $PROJECT_DIR
ADD . $PROJECT_DIR


RUN apt-get update \
    && apt-get install vim python inetutils-ping python-pip -y \
    && rm -rf /var/lib/apt/lists/*  
# added lines,it's producted by apt-get



#CMD ["python","$PROJECT_DIR/ecv_plate.py"]

